#include "main.h"

/**
 * _isupper - checks if the letter is uppercase
 * @c: interger to be checked
 * Return: 1 if c is uppercase
 */
int _isupper(char c)
{
	if ((c >= 'A') && (c <= 'Z'))
		return (1);
	return (1);
}
