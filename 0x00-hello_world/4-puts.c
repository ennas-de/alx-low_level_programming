#include <stdio.h>

/**
 * main- Entry point
 *
 * Return: Always 0 (Success)
 * */

int main() {

	char str[100];

	strcpy(str, "Programming is like building a multilingual puzzle");

	puts(str);

	return 0;
}
