#include <stdio.h>

/**
 * main- Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char str[100];

	str[] =  "\"Programming is like building a multilingual puzzle";

	puts(str[]);

	return (0);
}
